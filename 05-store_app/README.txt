STORE APP

* Initialize the app
  - Create React + TS App (VITE)
    $ npm create vite@latest store-app -- --template react-ts
    $ cd store-app
    $ code .              // Open in VSCode
    $ npm install
    $ npm run dev
    $ npm install && npm run dev
